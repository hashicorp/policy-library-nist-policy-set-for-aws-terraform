# Copyright IBM Corp. 2025
# SPDX-License-Identifier: BUSL-1.1

import "strings"

providers = {
	"aws": {
		"alias": "",
		"config": {
			"region": {
				"constant_value": "us-east-1",
			},
		},
		"full_name":           "registry.terraform.io/hashicorp/aws",
		"module_address":      "",
		"name":                "aws",
		"provider_config_key": "aws",
		"version_constraint":  "",
	},
}

resources = {
	"aws_kms_key.example": {
		"address": "aws_kms_key.example",
		"config": {
			"deletion_window_in_days": {
				"constant_value": 7,
			},
			"description": {
				"constant_value": "KMS key for Redshift Serverless namespace encryption",
			},
			"enable_key_rotation": {
				"constant_value": true,
			},
		},
		"count":               {},
		"depends_on":          [],
		"for_each":            {},
		"mode":                "managed",
		"module_address":      "",
		"name":                "example",
		"provider_config_key": "aws",
		"provisioners":        [],
		"type":                "aws_kms_key",
	},
	"aws_redshiftserverless_namespace.example": {
		"address": "aws_redshiftserverless_namespace.example",
		"config": {
			"kms_key_id": {
				"references": [
					"aws_kms_key.example.arn",
					"aws_kms_key.example",
				],
			},
			"namespace_name": {
				"constant_value": "concurrency-scaling",
			},
		},
		"count":               {},
		"depends_on":          [],
		"for_each":            {},
		"mode":                "managed",
		"module_address":      "",
		"name":                "example",
		"provider_config_key": "aws",
		"provisioners":        [],
		"type":                "aws_redshiftserverless_namespace",
	},
}

provisioners = {}

variables = {}

outputs = {}

module_calls = {}

strip_index = func(addr) {
	s = strings.split(addr, ".")
	for s as i, v {
		s[i] = strings.split(v, "[")[0]
	}

	return strings.join(s, ".")
}
